<% layout('../layout/main') -%>

    <h1>Hello World!!!</h1>
    <p>Welcome to <%= title %></p>
    <% users.forEach((user) => { %>
        <a href= "/users/<%- user._id %>"><%- user.username %></a>
        </br>
        <% }) %>